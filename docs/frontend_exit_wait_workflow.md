# Frontend exit wait recovery

`nba97_frontend_exit_wait` recovers FEONLY `0x8002EFBC..0x8002F083` from the 200-byte source range whose SHA-256 is `45eed4157e3ece4487b1c0c8ea03ed780461937168a8d38e05853200ebf6ad53`. It executes all 50 source instructions with MIPS delay-slot order, retained guest memory, byte-granular knownness, full 32-GPR/HI/LO state, and operation/access/instruction journals.

The routine loads the frontend handle at `0x80017268` before changing SP. It then creates a 24-byte frame, sets V0 to `-1`, saves RA, compares the original handle with the sentinel, and saves S0 in the branch delay. The sentinel path therefore still performs both frame stores before restoring and returning.

A live handle calls `0x8007B2BC(handle,100,-1)`, reads the clock at `0x8008DA5C`, and forms the deadline as callback-returned V0 plus 360 with 32-bit wrap. Each loop calls `0x8006B6A0` and latches its old V0 for the signed BLTZ before the delay forces V0 to `-1`. If that result is nonnegative, `0x8006FCF0` supplies the old V0 for BNE before its delay again forces V0 to `-1`. A zero result calls `0x80039260`, reads the clock again, and repeats while the signed callback-live S0 deadline is greater than or equal to the current clock.

Deadline expiry reloads the handle for `0x80092C34`. Every non-sentinel exit stores `-1` to `0x80017268`, calls `0x80028C28` and `0x8006FAA0`, loads `0x8002149C` for `0x80028CF4`, and clears that global only after the callback. All ten child sites remain typed FEONLY callbacks. Existing semantic owners cover voice handles at `0x8007B2BC`/`0x80092C34` and the music status table used by `0x8006B6A0`, but their narrow APIs do not carry the retained guest state and complete output machine required here. The two clock calls are separate source sites.

The explicit exit path reports sentinel, negative first poll, nonzero second poll, or deadline expiry. Poll conditions use the callback result captured before the delay-slot overwrite. Deadline addition wraps, signed comparisons preserve partial knownness, and callbacks may mutate S0 or relocate SP. Final RA and S0 loads use callback-live SP. Failures retain all completed memory and callback effects.

`nba97_frontend_exit_wait_from_frontend_exit_cleanup` accepts only the committed cleanup caller at JAL `0x8002F08C`, delay `0x8002F090`, target `0x8002EFBC`, invocation one, zero arguments, FEONLY identity, and known RA `0x8002F094`. Natural integration executes the existing cleanup C owner, routes its first child through this adapter, and keeps its other four children as explicit synthetic services. It also relocates the complete 48-byte wait/cleanup live frames with and without a knownness plane.

`nba97::captureFrontendExitWait()` returns JSON with stable top-level fields `program`, `address`, `inclusive_end`, `bytes`, `instructions`, `source_sha256`, `completed`, `accepted`, `result`, `contract_failure`, `classification`, `gameplay_shown`, `fixture_contract`, `parent_call`, `branch_state`, `memory`, `wait`, `final_machine`, and `next_unbound_boundary`. The deterministic deadline path reports 19 operations, nine accesses, ten callbacks, all 50 instruction PCs, exact access order, and ten complete call-boundary machine snapshots. Synthetic callbacks return clock `1000`, poll values zero, and clock `1361`; they preserve all other GPR words/masks and HI/LO and perform no memory effects.

Focused tests cover all source PCs and sites, exact arguments and delays, sentinel and all three live-handle exits, pre-delay condition latching, signed equality and wrap, callback-live deadline mutation, bounded loops, fresh global reloads, every operation budget and callback refusal, partial/malformed/absent knownness, initial-load-before-frame failure, stack alignment/mapping, all saved-RA masks, live-SP relocation, and global/frame aliases. Integration covers natural cleanup composition, nested refusal, invalid binding reuse, exact adapter guards, combined frame relocation, and capture serialization.

Gameplay shown remains `BLOCKED`. The earliest production boundary is still overlay entry `0x8007B838` and startup `0x80028810 -> 0x8007B844`. The next missing boundary inside this routine is the full-machine binding for `0x8002EFDC -> 0x8007B2BC`; reuse of the existing semantic voice owner is blocked on retained-state and output-machine transport. The remaining poll, clock and release targets are also typed services. After this owner, cleanup continues through `0x8002F094 -> 0x800394D4`. Actual loader handoff and an advancing native court/player loop remain unbound.

Manager validation: final MSVC Debug focused test passed 176 checks; natural integration/capture passed 19,862 checks. All 395 asset-free Debug CTest tests passed. The independent private original-instruction comparison passed 13,216 cases across all 50 PCs and ten call sites, comparing full 2 MiB memory, 34 machine words/masks, callbacks, accesses, instruction traces, operation budgets, and refusal prefixes. Progress/recovery/semantics/roster freshness checks passed. The native input-driven visual run captured 108 frames at .local/verification/team_select/game-entry-20260906-131556-a8a08685. Both wait boundary pixel hashes are 42378915a6f4b3706f54ada89180d18f2a570fe937baabf1f702191a0fc825d7. Its frontend_exit_wait_verified.json records the CPU-only state changes and explicit production blocker. Gameplay shown: BLOCKED.
