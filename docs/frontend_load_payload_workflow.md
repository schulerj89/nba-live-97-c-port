# Frontend loaded-payload result recovery

`nba97_frontend_load_payload` owns the complete FEONLY range `0x8007B15C..0x8007B18F`: 52 bytes and 13 instructions with source SHA-256 `aaf6935467d7d6bad48e084fafaf71528d7b8e6ebb23deca4bef4e2f2f9b3ebf`. It allocates a 24-byte frame, saves RA, and calls the unresolved `0x8007B1D0` loader with live A0, A1, and A2. Fresh evidence from `0x8007B1D0` confirms that all three argument registers are consumed even though the immediate caller's decompiler annotation omits formal parameters.

After the child returns, the source latches V0 before its branch-delay NOP. A fully-known null descriptor produces fully-known V0=0. A nonnull descriptor causes one little-endian word load through the returned guest address, retaining the payload's byte knownness in V0. A partially known pointer with a known nonzero byte can prove the branch is taken but still fails address resolution at `0x8007B17C`. Unknown null decisions, descriptor alignment and mapping failures, malformed knownness, child refusal, and operation limits retain exact completed access, callback, and instruction prefixes.

The saved-RA store occurs before the child. The payload load, when selected, precedes the callback-live stack restore. Descriptor memory can therefore alias the saved-RA word, and a relocated callback frame controls the final restore. SP arithmetic wraps at 32 bits. The owner preserves every callback-returned GPR and HI/LO field except source-written V0, restored RA, and adjusted SP. A partially known payload may return normally through a known RA. Conversely, a partial or misaligned saved RA faults only after the null or payload result and final SP adjustment are complete.

`nba97_frontend_load_payload_from_frontend_overlay_load` accepts only the committed recovered parent's `0x8007B124` call, delay `0x8007B128`, target `0x8007B15C`, RA `0x8007B12C`, invocation one, and argument count three. It also verifies the parent's delay-forced fully-known A2=1 while allowing live A0/A1 values and knownness. `nba97_frontend_overlay_load_with_recovered_payload` genuinely composes the committed overlay-load owner with this owner and forwards no invented child ABI.

`nba97::captureFrontendLoadPayload()` returns printable JSON with stable top-level fields `program`, `address`, `inclusive_end`, `bytes`, `instructions`, `source_sha256`, `completed`, `contract_failure`, `classification`, `gameplay_shown`, `fixture_contract`, `paths`, and `next_unbound_boundary`. Its two paths begin from explicit synthetic standalone overlay-load machines and retained memory, then execute actual overlay-load to loaded-payload composition. The unbound `0x8007B1D0` fixture validates A0/A1/A2 and preserves the full CPU and memory, changing only V0 to fully-known zero or descriptor `0x80170000`. The descriptor is preseeded synthetic retained input containing `0x801E1410` with known mask 7. Each path reports complete parent and owner PC/access/call journals, exact boundary machines, result knownness, and final machines.

Strict local validation compiles the focused C11 owner and the C++20 natural integration with MSVC `/W4 /WX`, placing all outputs under the ignored root path `.local/build/frontend_load_payload_worker`. Focused coverage includes both branches and all 13 PCs, all 16 child-pointer masks, all 16 payload masks, unknown decisions and addresses, frame/descriptor aliasing, callback relocation, argument forwarding, every operation-budget prefix, absent and malformed knownness planes, region/alignment failures, wrapping and partial SP, child refusal/malformed output, late RA faults, callback machine preservation, and deterministic repeated reads. Integration covers both actual parent paths, combined-frame relocation, nested failures, exact typed guards, rejected-binding reuse, and printable capture serialization.

The next exact boundary is `0x8007B164 -> 0x8007B1D0(a0,a1,a2)`, whose filesystem and descriptor behavior remains an unbound full-machine service. The other caller at `0x8007B144` is also unowned. Actual filesystem-backed overlay loading, the GAMELOAD lifecycle, and an advancing native court/player match remain required before gameplay or tipoff can be claimed.

Manager validation passed after exclusive worker freeze: final MSVC Debug focused 165 checks, natural integration 15,535 checks, and all 407 asset-free CTest tests (31.56 seconds). The private raw-instruction differential passed 42,240 cases across all 13 source PCs, comparing all 34 CPU words/masks, complete retained bytes/knownness, and exact access, callback, and instruction journals. Progress, recovery, instruction-semantics, and roster freshness checks passed.

The native input-driven verifier captured 120 frames in ignored run `.local/verification/team_select/game-entry-20260906-142530-2e4f82a1`. Its `frontend_load_payload_verified.json` validates both actual parent paths, full machines and journals. Before/after native PPM pixel SHA-256 both equal `42378915a6f4b3706f54ada89180d18f2a570fe937baabf1f702191a0fc825d7`. Gameplay shown: BLOCKED at the typed `0x8007B164 -> 0x8007B1D0` service and subsequent filesystem-backed GAMELOAD lifecycle.
