# Trade Players: entry progress

Generated by `python tools/verify_trade_rosters.py`. Do not hand-edit.

| Narrow entry scope | Accounted | Pending |
|---|---:|---:|
| `80056CD0` wrapper | 39 / 39 | 0 |

**100.00% of this wrapper is source-accounted, not percent of Trade Players complete.**
Static reporting does not run tests or prove original execution equivalence.
The Trade card now opens a native screen. Its host/asset/save regression is
separate: `python tools/verify_trade_screen.py` (requires private assets).
Working integration does not automatically earn instruction-block credit.

39 wrapper instructions source-accounted: right-then-left normalization and LEFT collision repair; two kind1 constructor arguments; actual native first/second callback bindings; signed halfword selector return used by host child/exit routing. Native ABI and owned async state replace the original stack/blocking selector. Explicitly scoped callee contracts do not credit callee bodies, transaction-boundary parity, or original timing.

Only the39-instruction wrapper is the denominator. Native Trade integration is tested separately by verify_trade_screen.py. Read-only Ghidra now resolves callback80056C50 as32 instructions; constructor276, first-input67, scan115, View-return79 and Compare-return100 remain outside the counted scope until per-block review. Existing Re-order credit is not duplicated. Original runtime timing/audio/video fidelity remains unverified.

## Tested call contracts, not callee-body coverage

| Original helper | Native contract | Outside this claim |
|---|---|---|
| `0x80056A94` | Signed sentinel-team normalization called right then left, mode/context retained. | Existing shared helper body receives no additional Trade instruction credit. |
| `0x80056494` | Normalized distinct teams, two kind1 lists, selected callback pair,535-slot entry snapshot, saved viewports and signed selector-result transport through the async host. | Not the full276-instruction constructor: allocation/draw ordering, original reentry snapshot boundary, special-mode eligibility contexts and exact presentation timing remain separate. |
| `0x80056B44` | Actual first-list callback: View/Compare requests, injury guard then first selection; empty receiver allowed. | Not the full67-instruction body or exact callback/poll/sound timing; native sentinel safety avoids invalid injury reads. |
| `0x80056C50` | Actual second-list callback: View/Compare requests, shared validation/mutation with kinds1/1, and return to first selection. | Not the full32-instruction callback body or transitive helper coverage; exact timing/audio remains open. |

The original blocking call is an owned native controller plus a frame pump.
The host consumes signed selector results:1 accept,-1 cancel,2 View,3 Compare.
This wrapper accounting does not settle the original post-child rollback
boundary or prove exact input, audio, animation or rendering fidelity.

See [implementation plan and evidence](trade_rosters_workflow.md).

```powershell
./scripts/verify_trade_rosters.ps1
python tools/verify_trade_rosters.py --check
```
