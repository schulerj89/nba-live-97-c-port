# GAMEONLY text-chain clear recovery

This recovery owns only `0x8003066C..0x800306E7`, 124 bytes and 31 instructions. Its source is the fresh complete Ghidra listing `game_8003066c.txt`, instruction SHA-256 `cc232ba81dfc988aabbaaa955e1d2c257f0634f49e6e49520c7ee3f1333e1746`. The listing identifies 27 call sites; the natural composition in this change uses the already recovered countdown caller at `0x8003295C`, delay `0x80032960`, return address `0x80032964`, with `a0=0xC9`.

The leaf always reads the font pointer at `0x800B2048`, even when the signed low half of `a0` is negative. A nonnegative index loads the head table through `font+0x14`. Each visited link reloads the link base through `font+0x10`, reads its successor at `link+0x18`, and clears `link+0x12` in the source BGEZ delay slot. That clear therefore remains visible for a terminal or unknown successor. The final path reloads the possibly aliased head-table pointer and stores `0xFFFF` to the original indexed cell. The source has no cycle guard; the native operation budget reports the exact retained prefix.

The C99 owner transports all 32 GPRs, HI, and LO with per-byte knownness and uses validated retained mapped memory for every wrapping guest address. It preserves source load/store order, alignment and resource failures, atomic load destinations, and the JR NOP delay. No guest address is cast to a host pointer and no retail data is embedded.

The adapter accepts only the exact countdown event, known `a0=0xC9`, and known `ra=0x80032964`. Its wrapper runs the production countdown owner on the same memory and full machine, forwards the countdown's other typed services unchanged, publishes stopped child machine prefixes, and promotes the child's typed failure result. Binding telemetry is reusable across parent runs.

Focused runtime-generated tests cover signed-low-half boundaries, ignored high halves, all 16 `a0` known masks, empty `0x8000`/`0xFFFF` heads, one and multiple links, partial successor masks, delay-slot stores, cycles and every finite operation cutoff, mutable base/table aliasing, mapped wrap, alignment/resource failures, malformed late bytes, all return-address masks, JR alignment, invalid and overlapping regions, `SIZE_MAX`, full-machine preservation, and repeatability. The natural test proves actual countdown call metadata, shared-memory chain and cache clearing, binding reuse, malformed-event guards, forwarding of unresolved text submission, and budget/resource/unknown failure prefixes that prevent the parent cache overwrite.

Manager differential validation passed 8,128 cases across all 31 PCs, full 34-word machine state and masks, full 2 MiB RAM, generated chains, cycles, signed indices, and operation prefixes. Focused tests pass 179 checks, actual countdown integration passes 17, and the complete suite passes 371 tests. The self-driving native capture composes actual frame UI, countdown, and text clear owners on one memory. Slot 201's two links are cleared and its head changes 3 to 65535 before the countdown cache store. The receipt proves 39 instructions, 11 operations, eight reads, and three stores; comparison frames are identical and the visible frontend remains User Setup.

Gameplay shown: **BLOCKED**. This owner changes retained text-chain state but does not render it. Visible UI evidence requires the linked text renderer and countdown submission services to be recovered and composed by their separate owners.
