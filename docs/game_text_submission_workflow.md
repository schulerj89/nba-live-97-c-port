# GAMEONLY text submission recovery

This recovery owns only `0x80030D18..0x80031523`, 2,060 bytes and 515 instructions. The source is the fresh complete Ghidra listing `game_80030d18.txt`, instruction SHA-256 `5bb63d6349c6c14fc92caa3cfef8326b9d02eb4b87d6a9477ee9afcbae97daec`. The selected natural caller is the recovered countdown owner at call `0x800329E8`, delay `0x800329EC`, return address `0x800329F0`, with four register arguments and the live fifth stack argument recorded in the source ledger.

The C99 owner transports all 32 GPRs, HI, and LO with per-byte knownness. Every guest address uses validated retained mapped memory with explicit access and child-call budgets. The owner preserves the original 0x80-byte callback-live frame, allocation search and exhausted-pool wrap, signed slot caches, four retained list modes, two ordering-table heads, runtime character-bank controls, runtime JR dispatch table, glyph packet writes, backward packet linking, and epilogue reloads through callback-live `sp`.

All direct calls remain typed at their exact source PCs and delay slots. The production adapter composes the two `0x80099960` calls through the existing clear-ordering-table owner because that owner carries a compatible full machine and mapped-memory contract. Its dynamic debug/backend services remain typed inputs. The existing `0x800AA468` memory-copy API reports memory-copy state but not its full source machine, so this boundary remains typed; the adapter does not invent unreported register preservation or copy its algorithm. `0x8002EB50`, `0x8002EF88`, `0x8002ECD4`, and `0x80056914` also remain typed.

The countdown adapter validates the exact event, all register arguments, known `ra=0x800329F0`, and the known fifth word at live `sp+0x10`. It invokes text submission on the same retained memory and full machine, publishes stopped prefixes, and forwards other countdown services. Binding state records the text owner and both composed ordering-table invocations and remains reusable across parent runs.

Asset-free focused fixtures generate font, record, head, character, glyph, string, packet, and stack bytes at runtime. They exercise free-first, free-later, wrapped, and exhausted allocation; null allocation; every list and alignment mode; signed slot boundaries; controls, fallback glyphs, and all five newline dispatch targets; repeated glyph packets; every operation cutoff on empty and visible-glyph paths; child refusals; all return-address masks; malformed and unknown memory/address states; indirect-target unknown, alignment, and refusal outcomes; unknown stores without a knownness plane; `SIZE_MAX` validation; and deterministic machine, memory, and access journals. The natural fixture runs the actual countdown call, two actual clear-ordering-table owners with synthetic typed backend responses, and the later typed countdown upload on one shared machine and memory image. It also checks every exact caller identifier, accepted malformed child machines, both ordering-table refusal prefixes, parent prefix publication, and wrapper reuse.

Gameplay shown: **BLOCKED**. This routine creates UI text records and packets, but visible text still requires the unresolved measurement, allocator, packet-copy, packet-link, and graphics backend services. Synthetic packet fixtures are state evidence and are not rendered gameplay.

Manager comparison matched 16,416 private original-instruction cases, visiting all 515 instructions and matching all 34 machine words and output masks, full mapped RAM, callback-entry state, 32 stack words and operation-budget prefixes. Native capture composes actual countdown, text submission, two clear-table owners, image-record upload and rectangle submission on the same generated RAM. The empty-string record claims slot 201 and changes its chain head from FFFF to 0; the native User Setup pixels remain identical. Typed prerequisite responses are fixture contracts, not original graphics completion or gameplay.
