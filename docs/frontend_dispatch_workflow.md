# FEONLY frontend dispatcher recovery

This recovery owns only FEONLY `0x8003F7C8..0x80040A1B`: 4,692 bytes and 1,173 instructions. The fresh Ghidra listing is `feonly_8003f7c8.txt`; its instruction bytes have SHA-256 `a42d7d2d97ab00ad7ddb214677b743dfd5d98d05119f9e6894fd092a6ccf1b9f`. The sole evidenced caller is the unowned wrapper at `0x800360D4`, whose `JAL` is at `0x800360F4`, NOP delay at `0x800360F8`, and return address `0x800360FC`.

`nba97_frontend_dispatch` translates the whole source routine. It initializes the retained frontend context, follows the 43-entry runtime jump table at `0x80024F80`, executes every state transition with source delay-slot order, preserves the 14-byte custom-rule rollback, performs cleanup, dispatches the 24 roster-copy calls across 12 iterations, and restores `ra/fp/s7..s0` from callback-live `sp`. The machine contract carries all 32 GPRs plus HI/LO and a four-bit per-byte knownness mask for every word. Memory remains a validated set of 32-bit guest regions; no host pointer represents a guest address.

Each of the 79 calls is a synchronous typed event. `nba97_frontend_dispatch_site_contract` exposes the exact source call PC, delay PC, target and argument count used to whitelist a concrete fixture or future owner composition. The callback receives the complete mutable machine after the delay slot. A refusal, malformed returned machine, unknown branch, invalid byte, alignment trap, unsupported runtime jump, or operation-budget stop publishes the precise reached prefix. The combined budget counts attempted mapped-memory accesses and typed calls; ordinary CPU instructions remain finite except through the source menu loop.

`nba97_frontend_dispatch_from_800360d4` is the narrow production entry adapter. It requires an explicit full machine and validates the exact `360F4/360F8/3F7C8/360FC` boundary. The wrapper has no recovered full-machine owner, so the adapter does not invent caller-saved registers, HI/LO, or formal arguments.

The integration fixture composes `UserSetupSession` only at the exact `0x80037010` child boundary. Native Start acceptance supplies the genuine recovered result `6` and assignments. Every other menu, presentation, rules, allocation, cleanup and roster child remains an exact-site synthetic prerequisite or a bounded typed copy fixture; those responses are fixture contracts, not recovered upstream behavior.

`nba97::captureFrontendDispatch()` returns JSON without writing files. It records the program/range/hash, entry boundary, result and counters, launch/mode/buffer and roster checksums before and after, native User Setup result and assignments, and the full child sequence (`pc`, `target`, `delay`, `argc`, `invocation`). Its fixture accepts only the exact 42-call sequence across the 20 source sites used by this path, validates all 24 `909A8` source/destination/count triples, and preserves the full machine except for each documented synthetic V0 result. The capture is a CPU/UI probe. It does not render a pregame or gameplay frame.

The next source path is still unbound: dispatcher return `0x800360FC`, wrapper return `0x80028AA8`, FEONLY load of `gameload.bin` at `0x80028ACC`, copy to `0x801E0000` at `0x80028B54`, then dynamic transfer at `0x80028B68`. FELOAD and GAMELOAD reuse addresses in distinct overlays. Native `deferMatch()` remains the deliberate pending policy until that host exit and loader chain is recovered and wired. Visual classification is UI/menu; gameplay shown remains blocked.

The focused suite uses only generated memory, knownness planes and symbolic dispatch destinations. Its 284,665 checks cover all 43 state entries, source branch regressions, typed call contracts, User Setup success/cancel paths, rollback and roster cases, partial knownness, malformed late bytes, aliases, operation budgets, callback refusal and deterministic replay. The integration suite covers the exact unowned caller adapter, negative guards, reuse, callback-live stack relocation and actual `UserSetupSession` acceptance. No retail table, executable asset, binary fixture, image or audio asset is required.

Manager review compared the complete C owner against fresh original instruction evidence. The private differential passed 13,776 cases covering all 1,173 source PCs, full 2 MiB retained memory, all 34 machine words and knownness masks, callback stack snapshots, mutable stack/register state and operation-budget prefixes. The telemetry-only backup-loop counter correction was followed by focused tests and repeated pilot/directed comparisons. Strict Clang and the MSVC Debug build passed 284,665 focused checks and 248 integration checks. The Debug run also verified that the synthetic copy callback skips the absent knownness plane, avoiding an out-of-bounds test-fixture access.

The existing self-driving native verifier now captures `frontend-dispatch-before` and `frontend-dispatch-after` at the accepted User Setup boundary through the port's native input APIs. It checks the exact 42-call fixture sequence and mode/buffer/roster receipt, then requires pixel-identical menu frames. The capture is a separate synthetic CPU probe; the live host still defers the match. Captured logs, frames and verification receipts remain ignored.

Gameplay shown: BLOCKED. The native frontend lifecycle and the FEONLY `0x800360D4` return chain to the `0x80028B68` GAMELOAD transfer remain unbound. The displayed User Setup screen is not advancing gameplay. Work stops after this routine's verified push at the user's request; the next wrapper is not started.

Final manager validation: the MSVC Debug build passed all 387 asset-free CTest tests in 10.02 seconds, including 284,665 focused checks and 248 integration checks. Progress, recovery metadata, instruction-semantics freshness and roster scenario configuration checks passed. The native input-driven run `game-entry-20260906-110321-91a64d75` verified 100 captured states, the exact 42-call dispatcher receipt, and identical before/after User Setup frame SHA-256 `42378915a6f4b3706f54ada89180d18f2a570fe937baabf1f702191a0fc825d7`. The manager inspected the local screen proof. Captures and differential evidence are ignored local files and are not part of the recovery commit.
