# FEONLY frontend exit drain recovery

`nba97_frontend_exit_drain` recovers FEONLY `0x800394D4..0x80039573` from the complete 160-byte, 40-instruction source range whose SHA-256 is `5b71620fae4987715d545936b770ac78df30d0b8501120fd3ae5e3abb1a61617`. Fresh Ghidra evidence establishes the sole observed caller at `0x8002F094` in the already recovered frontend exit cleanup. No complete owner existed before this recovery.

The owner reads the active flag at `0x800F84C4` before subtracting 24 from `sp`. Its early branch always saves entry `ra` at the new `sp+16` in the delay slot. A zero flag then restores and returns without calling a child. A nonzero flag calls `0x800393F0`, repeatedly calls `0x800392A0`, and calls `0x80038E84` after each zero poll result. The source loop has no bound; the native operation budget reports `NBA97_TEXT_LIMIT` at the exact next attempted callback while retaining the completed prefix.

After a nonzero poll, the routine reads `0x8002149C` into `v0`, clears `0x800F43B0` and `0x800F84C4` in that order, and clears `a0` in the branch delay on both paths. A nonzero first mode snapshot calls `0x80029B64` with known `a0=0` and clears `a1=0` in the JAL delay. The routine always calls `0x8008C274`, then independently reloads `0x8002149C` into `a0`. A nonzero second snapshot calls `0x8006CDE4` with that one argument, followed by the zero-argument `0x8006AE60`. Fresh child evidence confirms that `0x8006CDE4` consumes `a0`; it is not treated as a zero-argument service.

All seven child routines remain typed callbacks. They receive the exact call PC, delay PC, target program, target entry, invocation, argument count, and complete mutable 32-GPR/HI/LO machine. Every JAL publishes known `ra=pc+8` after its delay-slot work. Successful callbacks may synchronously mutate retained bytes, register words and masks, HI/LO, and live `sp`; invalid callback machine state and refused calls preserve the exact reached prefix. Guest words use validated little-endian regions with optional per-byte knownness. Misalignment, missing spans, malformed knownness, unrepresentable unknown stores, overlapping or wrapping regions, unknown decisions, operation limits, and final return alignment remain explicit results.

`nba97_frontend_exit_drain_from_frontend_exit_cleanup` is the natural-caller adapter. It accepts only FEONLY site `0x8002F094`, delay `0x8002F098`, entry `0x800394D4`, invocation one, zero formal arguments, and fully known `ra=0x8002F09C`. The integration fixture executes the existing `nba97_frontend_exit_cleanup` C owner and routes its second child through this adapter. The first cleanup child now executes the committed wait owner with an explicitly retained -1 handle, so it takes its source sentinel path without any invented child ABI. Drain and later cleanup dependencies remain explicit synthetic fixtures. A relocation case moves both nested 24-byte frames and proves that the drain and cleanup restore their own return words through callback-live `sp`, with and without a knownness plane.

The standalone capture uses generated 2 MiB memory and no retail asset. Six child fixtures preserve all 32 GPR words and masks plus HI/LO and perform no guest-memory effects. The polling fixture changes only `v0`, returning known zero and then known one. Its resulting path records all 40 unique source PCs, eight call events across all seven sites, seven ordered guest accesses, both mode snapshots, full callback-entry machines, and the final machine. These fixture values document the test contract only; they do not establish a production ABI or implement any child service.

The focused MSVC `/W4 /WX /Od /sdl` executable passes 204 checks. It covers the inactive and active exits, all 40 PCs and seven sites, repeated and partially known poll results, every operation-budget prefix, callback refusals, independent mode reload changes, delay-slot argument clears, initial-read ordering, partial and malformed knownness, absent knownness planes, stack wrap/alignment/mapping failures, region overlap/wrap guards, saved-`ra` aliasing, callback-live frame relocation, return knownness/alignment, and mutable GPR/HI/LO state. The strict natural-caller and capture executable passes 17,718 checks. Manager-owned independent raw-instruction comparison passed 29,440 cases over the full 2 MiB RAM, machine state, all PCs and call sites, ordered calls/accesses, operation budgets, and refusal prefixes; its private receipt remains under `.local/evidence/`.

Visual classification is `no direct visual effect`. Gameplay shown is `BLOCKED`: the capture is a CPU-only synthetic receipt, while the earliest production overlay-entry/startup boundary and the actual advancing native match handoff remain unbound. No `deferMatch` bypass or fabricated production service is introduced.

Final manager validation: MSVC Debug focused 204 / natural integration 17,719 checks passed; the latter now composes both committed wait and new drain through the cleanup owner. All 397 asset-free Debug CTest tests passed in 33.68 seconds. Progress/recovery/semantics/roster checks passed. Native input verification captured 110 frames at .local/verification/team_select/game-entry-20260906-132324-8fe6ea51; frontend_exit_drain_verified.json proves the ordered state changes and both boundary pixel hashes 42378915a6f4b3706f54ada89180d18f2a570fe937baabf1f702191a0fc825d7. Screenshot proof is local only. Gameplay shown: BLOCKED; next drain child is 0x800394E8 -> 0x800393F0 and the production lifecycle/match loop remain unbound.
