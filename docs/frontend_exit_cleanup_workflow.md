# Frontend exit cleanup recovery

`nba97_frontend_exit_cleanup` recovers FEONLY `0x8002F084..0x8002F0E7` from the 100-byte source range whose SHA-256 is `38b3b7e879958bf82f3c214dea99f4b4bdb0c69f77eae68ae32692c7c9da29ec`. It executes all 25 instructions with source delay-slot order, full 32-GPR/HI/LO state, byte-granular knownness, retained guest memory, and operation/access/instruction journals.

The routine saves RA in a 24-byte frame, calls `0x8002EFBC` and `0x800394D4`, loads the raw signed selector word at `0x80021D6C` into A0 for `0x80028C90`, then independently reloads the resource pointer at `0x8001502C`. A nonzero resource calls `0x8007760C` with that A0 value and clears the global. Both branches call `0x80076540`, restore RA through callback-live SP, add 24 to callback-live SP, and return through the restored word. All five children remain typed FEONLY callbacks.

The conditional release call has one argument. Fresh callee evidence for FEONLY `0x8007760C` confirms that A0 is tested and consumed. The selector and resource values retain their complete words and known masks. A known nonzero byte chooses the release path even when other bytes are unknown; a partially known all-zero value stops as unknown after the branch NOP.

The source permits two material quirks. A child may relocate SP, so the final RA load uses the callback-returned frame. Guest mappings can also place entry-SP-8 at `0x8001502C`; the conditional clear then overwrites saved RA with zero, and the source returns to zero. Partial failures retain completed callback mutations, memory changes, access order, and machine state.

`nba97_frontend_exit_cleanup_from_frontend_main` accepts only the recovered main call at JAL `0x80028AA8`, delay `0x80028AAC`, entry `0x8002F084`, FEONLY program, invocation one, zero arguments, and known RA `0x80028AB0`. Integration uses the committed frontend-main owner and its existing wrapper/dispatcher composition. Main startup, loader, copy, GAMELOAD, and other unowned children use documented synthetic test services.

`nba97::captureFrontendExitCleanup()` returns JSON with stable top-level fields `program`, `address`, `inclusive_end`, `bytes`, `instructions`, `source_sha256`, `completed`, `accepted`, `result`, `contract_failure`, `classification`, `gameplay_shown`, `fixture_contract`, `parent_call`, `memory`, `cleanup`, `final_machine`, and `next_unbound_boundary`. The nonzero capture path records 10 operations, five accesses, five callbacks, all 25 instruction PCs, the exact access journal, and five call-boundary machine snapshots. The synthetic children preserve every GPR word/mask and HI/LO and perform no memory effects.

Focused tests cover zero and nonzero paths, all PCs and sites, exact arguments and delays, ordered four/five-access prefixes, all operation budgets, every callback refusal, callback mutations, partial and malformed knownness, absent knownness planes, stack alignment/mapping/wrap failures, all restored-RA masks, return alignment, live-SP relocation, and the saved-RA/global alias. Natural integration exercises main-to-wrapper-to-dispatcher, native `UserSetupSession` acceptance, main-to-cleanup, nested refusal and binding reuse, and relocation of the combined 64-byte cleanup/main frames with and without a knownness plane.

Gameplay shown remains `BLOCKED`. The earliest production boundary is still the unowned overlay-entry JAL `0x8007B838` and startup service `0x80028810 -> 0x8007B844`. Inside this cleanup, the next missing child is `0x8002F08C -> 0x8002EFBC`, followed by `0x800394D4`, `0x80028C90`, conditional `0x8007760C`, and `0x80076540`. Main loading-screen `0x80028AB0 -> 0x80028E08`, loader/copy, and GAMELOAD transfer remain separate dependencies.
The integration fixture keeps all unowned services explicit. Main startup publishes the launch flag at `0x80015098`; its two allocation callbacks return V0=`0x80140000`, the synthetic loader returns V0=`0x80170000`, and its size query returns V0=4096. The copy moves the complete generated buffer and its optional knownness plane to `0x801E0000`; the GAMELOAD callback returns. Other main services preserve the full machine. Dispatcher fixtures use the same retained acceptance path: allocation V0=`0x80140000`, state V0=5, the state setter writes context+`0x720`, native `UserSetupSession` produces V0=6, and roster copies move 110 bytes with optional knownness; other dispatcher callbacks return V0=`0xFFFFFFFF`, preserving all remaining registers and HI/LO. Cleanup's five fixture callbacks preserve the full machine and do no service work, except explicitly requested failure/stack-relocation test modes. These contracts are test prerequisites, not recovered production ABIs.

A rejected adapter binding updates its result but does not increment invocation/completion counts or replace prior progress. Consumers must gate progress on the result and invocation count; integration verifies this after successful reuse. Source-owner failures after entry publish the fresh executed prefix.
Final manager validation on 2026-09-06: the MSVC Debug all-target build passed, direct focused tests passed 146 checks, natural integration passed 12,045 checks, and all 393 asset-free Debug CTest tests passed in 33.41 seconds. The final argc-aware raw-instruction differential passed 6,464 cases across all 25 PCs and five sites, comparing full 2 MiB RAM, 34 machine words and masks, callback snapshots including argument counts, exact memory/PC journals, all operation budgets, and refusal prefixes. The final C source matches the compared snapshot. Progress generation/freshness, recovery registry, instruction-semantics freshness, and roster-configuration checks passed.

The native input-API visual verifier passed with 106 frames. The cleanup probe recorded all 25 instruction PCs, five child calls and five guest accesses, cleared `0x8001502C`, and restored the original return address and SP. Its five full-machine snapshots matched the explicit synthetic callback contract. Local ignored proof is `.local/verification/team_select/game-entry-20260906-123704-0ce8be2b/frames/frontend-exit-cleanup-after.png`, with receipt `frontend_exit_cleanup_verified.json` beside it. Both before/after pixel hashes are `42378915a6f4b3706f54ada89180d18f2a570fe937baabf1f702191a0fc825d7`. The inspected frame remains User Setup; the live application still defers the match.

Gameplay shown: BLOCKED. The next source child is FEONLY `0x8002EFBC..0x8002F083` (200 bytes/50 instructions), reached at `0x8002F08C`. Its fresh local range hash is `45eed4157e3ece4487b1c0c8ea03ed780461937168a8d38e05853200ebf6ad53`; it is not implemented in this commit. Production entry/startup, subsequent cleanup services, and the loading-screen/load/copy/GAMELOAD chain remain unbound. No live no$psx process was available and no fresh original runtime trace is claimed.
