# Re-order Rosters: small-slice progress

Generated by `python tools/verify_reorder_rosters.py`. Do not hand-edit.

Reviewed instruction accounting: **875 / 875 (100.00%)**; **0 pending**.

Next slice: **feedback**. Feature acceptance: **pending**.

This percentage covers only the initial ten-function inventory, not the whole feature or game. It is reviewed source accounting, not instruction execution, visual fidelity, or a binary match. Working fragments receive no whole-function credit. Native-tested means tests passed during the recorded implementation review; rerun them for current evidence.

| Slice | Status | Exit gate |
|---|---|---|
| 1. Team normalization and shared roster-list operations | native_tested | Shared validation/mutation owners and native contracts tested, including cross-mode synthetic branch cases; real Re-order uses these helpers. Other roster transaction screens are NOT thereby implemented. |
| 2. Two-step selection state machine | native_tested | All four selection owners source-accounted with tested Re-order call contracts: masks, two-stage focus, rejections/modals, row refresh, pulse/reset and cancel barrier. This is controller/diagnostic coverage, not full transitive or game-window completion. |
| 3. Original two-list screen and menu entry | native_tested | Real Rosters card5 enters state0x0C; private Z2PORT/ZSET4/font assets; 535-slot snapshot, 30 rows/six visible, saved selection, source-mapped entry/exit and native captures. Source/accounting tests pass; original image/timing parity remains the separate reference gate. |
| 4. Sound, highlights, help and View Player round trips | pending | Recovered sound IDs/timing, focus restoration, original-sized help, player return and exit tested. |
| 5. Local roster persistence and Reset integration | pending | Recover original commit/cancel semantics first; versioned local overrides, atomic save/reload, corrupt-file handling and Reset availability; never overwrite source assets. |
| 6. Original-reference acceptance | pending | Same-scenario original traces and screenshots; every relevant pending block resolved or explicitly justified as outside scope; transitive dependencies audited; known mismatches listed. |

## Instruction counts by area

| Area | Accounted | Pending |
|---|---:|---:|
| core | 296 / 296 | 0 |
| screen | 413 / 413 | 0 |
| interaction | 166 / 166 | 0 |

## Original instructions

| Function | Accounted | Pending |
|---|---:|---:|
| `0x80056A94` Normalize team | 22 / 22 | 0 |
| `0x80056AEC` Enter Re-order | 22 / 22 | 0 |
| `0x80056494` Construct/run two-list screen | 276 / 276 | 0 |
| `0x800568E4` First-list input | 54 / 54 | 0 |
| `0x800569BC` Second-list input | 54 / 54 | 0 |
| `0x800552B4` Begin second selection | 24 / 24 | 0 |
| `0x80055314` Finish/cancel second selection | 34 / 34 | 0 |
| `0x800556B0` Validate pair (shared) | 140 / 140 | 0 |
| `0x800558E0` Swap slots (shared) | 134 / 134 | 0 |
| `0x80055AF8` Refresh both lists | 115 / 115 | 0 |

## Selection call contracts (not full callee coverage)

Completing selection-owner blocks means their native call contracts are represented and tested. It does not transfer credit to shared callees or prove the game screen works. The 166-instruction selection denominator excludes these callees' own instructions.

| Original helper | Tested native contract | Integration still pending |
|---|---|---|
| `0x80054B94` | Re-order availability: active View ID, both Compare IDs; empty rejection; result2/3 plus working IDs. Same-player Compare is allowed. | Outer frontend View/Compare routing and child-screen round trips. |
| `0x80040A1C` | Two private message descriptors, blocking acknowledgement, original font, warning rectangle, growth/shrink and formatted child name. Shared validator emits typed injury/minimum/empty notices with original descriptor address and player/team subject. | Generic modal loop timing/raster comparison and sound5/8 playback in the game window; diagnostic is not the full modal engine. Trade/Sign/Release injury/minimum message presentation remains unconnected; no original message strings included. |
| `0x8002AB88` | Whole-row pulse using 2AB30 endpoints/flags and 2AE5C integer updates; actual pack-glyph modulation. | Generic text-object flags outside selection and original framebuffer/timing comparison. |
| `0x8002AC90` | Whole-row eight-update neutral transition via2AC2C, including duplicated blue write/start-red preservation. | Generic text-object effects and original game-window raster comparison. |
| `0x80055068` | Re-order-only refresh: team object0x73 at256,70 and natural-position line via54ECC/54DB4 at60/270,95; bench underscore. | Other frontend states and exact original screenshot parity; native Re-order portrait/background composition is now connected. |
| `0x8003B194` | Pump native color frames while held mask is unchanged; resume on changed input. Discrete CLI actions include release. | Physical controller sampling and generic repeat timing in the game loop. |

## Construction call contracts (not full callee coverage)

The 298 entry/construction instructions cover the Re-order specialization. Graphics state0x0C and input layout0x0D are deliberately separate. The original shared-engine bodies are not credited by this owner.

| Original helper | Tested native contract | Still outside the claim |
|---|---|---|
| `0x80030D14` | Bind private Z2PORT index/archive, decode87x51 PAL8 rows at88-byte stride; record0 fallback/playerN recordN+1; lazy portrait refresh. | No full asynchronous CD loader/cache instruction credit. Portrait asset hashes/captures require local screen verification; native core test alone cannot prove image decoding. |
| `0x80056128` | Re-order same-team descriptors: kinds1/2, bases0/15,15slots each, mode2 eligibility fallback; normalized first team feeds second. | Free-agent100-row and Trade/Sign descriptor paths not part of this Re-order specialization. |
| `0x80039D08` | Thirty retained type0x33 rows; positions60/270 and112+16n; pages0/1; endpoint navigation; team-scan and selection callback identities. | Other frontend object types and PSX GPU/text allocation not claimed. Hardware rasterization parity remains reference work. |
| `0x80055ED4` | Increment16 before returning the next baseline from initial96; restored top offsets represented in retained row geometry. | No standalone callee instruction credit in fixed inventory. |
| `0x8003D930` | Retained native frame pump replaces blocking engine; 0x0D controller restoration; entry560BC, exit56254, six-row input/selection/cancel and signed0/1/2/3 result contracts. | Full generic engine, held-key repeat, help, View/Compare dispatch, modal closing animation, exact audio/transition timing and original-reference comparison remain pending. |

## Shared-helper call contracts (not full callee coverage)

Each newly accounted shared block has a behavior note and named tests in the JSON report. These are reviewed mappings, not automatically proven MIPS equivalence. Cross-mode helper tests do not implement other transaction screens.

| Original helper | Tested native contract | Still outside the claim |
|---|---|---|
| `0x8004ED50` | Minimum-roster notice carries the affected team ID and original message address; C++ name lookup/presentation remains host-owned. | Generic city/name formatting and cross-mode minimum modal are not integrated. |
| `0x8005FE14` | Injury notice carries selected second-player ID, replacing record+0x29 name pointer; no guessed name or copied source text. | Cross-mode player-name formatting/modal and original provider execution evidence pending. |
| `0x800555F4` | Donor15/free100 compaction; starter repair scans ranked positions with mode/injury gate using caller-supplied private data; observes post-decrement count and original no-match0 behavior. | Real private preference/position provider tested across145 isolated starter transfers, plus synthetic injury cases. Trade/Sign/Release screens/providers remain unconnected. No standalone555F4/5539C instruction credit. |
| `0x8003B26C` | Redraw event emitted only for visible object IDs, native adapter consumes into retained row state and redraw diagnostics. | Generic PSX object redraw engine/raster not translated; retained compositor redraws its native surface. |
| `0x80039574` | Present-one-frame request between row/selected-ID updates and restored descriptor/header; native request counter consumed by retained host rendering. | Host presents asynchronously, not blocking original vblank; exact frame/audio timing and intermediate framebuffer equivalence pending. |

## Next work / pending blocks

## Run fresh verification

```powershell
pwsh -File scripts/verify_reorder_rosters.ps1
python tools/verify_reorder_rosters.py --check
```

57 asset-free scenarios and 7 optional local-database scenario are defined. The local scenario checks every ordered slot pair on all 29 teams (6,525 cases). The static ledger does not assert these tests just ran. The wrapper builds, runs fresh tests, and writes ignored CLI/evidence logs under `.local/`.

## Scope boundary

Fixed inventory:875 original instructions across ten owners, now fully reviewed source-accounted at explicit native call-contract boundaries. No denominator removed. Not binary matching, full transitive coverage, original execution proof, visual fidelity or whole-feature completion. Shared Trade/Sign/Release helper branches are synthetic-tested, but those screens and their private starter-preference data provider remain unconnected. Re-order entry/selection/refresh works. Help, View/Compare round trips, exact audio/frame timing, persistent overrides/Reset and original-reference acceptance remain separate pending gates.

Direct callees outside the fixed instruction inventory (scoped contracts above are not full body coverage; data callbacks also need audit):

`0x8002AB88`, `0x8002AC90`, `0x80030D14`, `0x80039574`, `0x80039D08`, `0x8003B194`, `0x8003B26C`, `0x8003D930`, `0x80040A1C`, `0x8004ED50`, `0x80054B94`, `0x80055068`, `0x800555F4`, `0x80055ED4`, `0x80056128`, `0x8005FE14`

The Re-order menu card now opens the original-asset two-list screen. Selection/swaps, team scans, accept and full-snapshot discard are connected. Help, View/Compare child routes, exact audio/transition timing, disk saving/Reset and original-reference acceptance remain pending. See [the workflow](reorder_rosters_workflow.md) before promoting a slice.
