# Camera phase selector recovery

`game_camera_phase_select.c` owns the complete GAMEONLY routine `0x8007E26C..0x8007E463`: 504 bytes and 126 instructions. The fresh Ghidra listing `game_8007e26c.txt` has instruction SHA-256 `e824731f1e58d3d3c6c6e664d93f2d0d6375f4a362a96eb863337521dbccd7bb`. Known callers are `0x8002DD8C` and the recovered camera selector at `0x80079A0C`.

The owner preserves the source control flow and delay effects. It reads the busy word before decrementing SP, spills RA even on the busy branch, and applies the low-byte A0 mask in the signed guard branch delay. A nonzero low byte clears the current and prior phase words. Match state 129 chooses phase 1. State 130 chooses phase 2 only when the signed threshold, period and score tests permit it. Other states retain current phase 3 or 4 and otherwise choose phase 3. Signed phase dispatch retains the source Boolean knownness and the A0 constants written by its branch delays.

A changed phase invokes the exact source children and arguments: phase 1 calls camera mode `(3,1)` followed by adjustments 15, 8 and 8; phase 2 calls `(7,0)` then adjustment 13; phases 3 and 4 call `(1,0)` or `(2,0)`, load the live byte at `0x80021ED8`, and finalize with that byte. The changed path clears `0x800FC99C`, rereads the callback-mutable current phase, and publishes it to `0x800BC944`. Unchanged and invalid signed phases still publish. The epilogue reloads RA through callback-live SP, advances SP by `0x18`, and executes the return delay NOP before validating live RA.

The strict C99 owner retains all 32 GPRs, HI/LO and per-byte knownness. It validates little-endian mapped memory, uses atomic loads, partial stores, known-null checks, exact access ordering, and a budget boundary for each memory access or child call. Its optional access journal records source PC, address, width, value, knownness and operation. Progress reports the selected and published phases, frame SP, restored RA, instruction count, call counts and exact refusal prefix.

`game_camera_phase_select_adapter.cpp` binds the actual recovered camera selector call at `0x80079A0C`, delay `0x80079A10`, entry `0x8007E26C`, RA `0x80079A14` and one argument. The parent event contract has no invocation field, so cumulative binding invocation counts remain telemetry and do not reject reuse across separate parent runs. The parent supplies either A0=0 or A0=1 according to its source busy branch; both are accepted. Because that parent exposes only GPRs, there is no source-proven incoming HI/LO state to copy, so the bridge marks both unknown. The nested recovered camera owner also exposes only GPRs and cannot make those words known; HI/LO remain unknown across that call unless a later typed full-machine child changes them. Any identifying event field, delay or known RA claims the assigned boundary before exact guards. A valid GPR mutation prefix survives a nested HI/LO metadata failure, while an invalid zero register or GPR mask is rejected without poisoning the narrow parent machine.

The inverse adapter composes the existing full recovered `game_camera_select` owner for all three `0x800799CC` sites. Calls to the unowned adjustment routine `0x80079EBC` and finalizer `0x80079F78` remain exact typed full-machine events. Independent budgets, journals and progress are retained for the CE owner and nested camera owner, and the wrapper resets per-run telemetry for safe reuse.

The focused executable performs 144 always-active checks. It covers busy and signed-guard exits; low-byte A0 behavior; phases 1, 2, 3 and 4; invalid negative and wrapping phases; signed threshold 2; period 0, 4 and other values; equal and different scores; changed and unchanged prior phases; all eight source call sites, arguments and order; every access/call budget prefix; every child refusal; callback-mutated SP, RA, HI/LO, registers and globals; unknown branch Boolean mask `0xE`; partial and malformed memory; known-null stores; aliases, stack wrapping, alignment, mapping and `SIZE_MAX`; unknown and misaligned return addresses; and deterministic full-machine preservation.

The natural executable performs 37 always-active checks through the actual recovered camera selector. It exercises both source-proven A0 values, inverse composition of the actual camera child, exact typed adjustment events, malformed parent identity and machine guards, nested refusal, invalid HI/LO with retained GPR prefix, owner budget exhaustion, missing mapped memory, null arguments and wrapper reuse. Both suites use heap-backed runtime-generated 2 MiB fixtures. They compile with strict C99/C++17 `-Wall -Wextra -Werror -pedantic-errors` and MSVC `/W4 /WX`; no runtime assets are required.

Production dependencies are the shared retained-memory/full-machine types and the recovered `game_camera_select` owner. The frame-pump caller at `0x8002DD8C`, adjustment child `0x80079EBC`, and finalizer `0x80079F78` remain outside CE.

Gameplay shown: **NO - no direct visual effect**. This routine updates retained camera-selection state and invokes typed camera configuration services. Visible camera behavior requires composition with the downstream renderer and live game path.




Manager validation: Clang C99/C++17 and VS2022 passed 144 focused and 37 natural checks; the full asset-free CTest suite passed 357/357. Private original comparison passed 8,064 cases, comparing all 34 full-known machine words and masks, 2 MiB RAM, child entry machines and stack, callback-mutated SP and phase globals, and every access/call budget prefix. It visited all 122 reachable instructions. The four retained jump/NOP instructions at 0x8007E368/36C and 0x8007E384/388 are unreachable under stable mapped memory: the preceding selection writes phase 1, 2 or 3, or retains only phase 3 or 4, and no child runs before dispatch.

Native run game-entry-20260906-072812-86bff6d4 records independent synthetic mode-zero invocations of the actual camera selector. Its current frontend startup uses mode 12; this probe does not claim that startup took mode zero or that a live frame-pump bridge exists. The unchanged case performs 10 operations (7 reads, 3 stores). The reset case performs 17 (7 reads, 6 stores, 4 calls), including the existing camera selector and typed adjustments 15, 8, 8. Both publish phase 1 and return through the natural parent frame at SP 0x801FEFA8; HI/LO remain unknown at the narrow GPR boundary.

Gameplay shown: NO - no direct visual effect. Native CPU before/after SHA-256 is 391c073b39664372b8277dcd1f82c30d946fc96300959d1bd09fe794f625d58d. The visible screen remains User Setup. Ignored local screenshot proof was inspected and is never committed.
