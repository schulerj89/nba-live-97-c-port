# Preserved original behavior

User instruction: keep bugs from the original game and comment them. Source
comments identify the original owner and observable behavior; this index makes
the confirmed cases easier to find. A surprising behavior is not automatically
a bug, so intentional-looking quirks remain described as such. Native port
defects should still be fixed.

| Original owner | Preserved behavior | Code and evidence |
|---|---|---|
|GAME65328 |Joined controllers retain their old selected-player halfword, including invalid values |`game_controllers.c`, controller tests and workflow |
|GAME653E8 |An existing player claim does not repair the controller's stale selected word |`game_controller_selection.c`, selection tests and workflow |
|GAME653E8 |No qualifying candidate reuses stale/incoming s6 and can select the wrong side or overwrite a claim |Same owner; incoming register provenance remains explicit |
|GAME7066C |Wrapped negation/shifts and signed comparisons retain INT_MIN/overflow results |Selection distance helper; full32-bit original comparisons |
|GAME65140 |An exhausted player in lineup position0 does not recover; later recovery counts increment even without a swap |`game_lineup_recovery.c`, recovery tests and workflow |
|GAME65140 |Status arithmetic saturates by the low16 sign bit, allowing wide sums to wrap to small positive values |Same owner; signed elapsed and overflow cases |
|GAME646A8 |Duplicate lineup entries use the last inverse-map write; entity subtraction retains raw underflow |`game_player_bindings.c`, bindings tests and workflow |
|GAME646A8 |Player byte+9 zero traps after earlier binding/entity writes; no reciprocal or later tail calls |Exact DIVIDE_TRAP prefix in bindings owner and original oracle |
|GAME914D8 |Rate zero traps after entering the critical section and installing cold handlers; a zero first quotient is stored before a second divide trap, with no rollback or critical-section exit |`game_clock_initialize.c`, clock tests and [clock workflow](game_clock_initialize_workflow.md) preserve both exact prefixes |
|GAME6459C/644FC |All-zero ratings retain carried register values, including a prior sort score as a player ID |`game_team_roles.c`, complete source tail-chain comparison |
|GAME64388 |Swaps do not advance the sort cursor; the result can remain incompletely sorted |Roles helper; exact source saved-return sentinel comparison retained |
|GAME56FFC force1 path |An in-range old frame/time can survive forced reset; primary mode2 copies secondary values without clamping |`game_player_initialization.c`, initializer/reset tests and workflow |
|GAME65DB0 lookup |A raw option outside the ordinary five still reads its original adjacent word |Private256-word lookup windows in `GameplaySetup`; no default duration |
|GAME60EF8 |Already sorted entities keep stale render indices; only actual insertion moves update the index halfword |`game_period_dependencies.c`, exact ordered sort writes and tests |
|GAME63EDC/51ED8 |Raw rating byte1E values23,24,25 or a zero FDB64 divisor trap after the height and attribute3A writes; raw entity IDs can alias height slots through the original shift wrap |`game_player_attributes.c`, exact trap prefixes and raw-ID source comparisons; no rating clamp |
|GAME56CE0/572C0 |Full motion queues silently drop; channels can diverge; producer searches exactFFFF while consumer treats any negative head as empty |Animation queue/advance owners and whole-source comparisons |
|GAME572C0 |Transitions discard remaining time; old primary header mode controls synchronization; blend promotion loses unrelated conversion bits |`game_animation_advance.c`, directed source and actual-resource replays |
|GAME57B18 |Foot-lock counter overflow into8000..FFFF reenters the early cache branch; unused B fields stay stale |Pose request owner and composed foot-lock tests |
|GAME55018 |Asymmetric Euler adjustment changes A even at weight0; full unsigned weights and unusual distance expressions remain |Gameplay pose sampler and raw-angle original comparisons |
|GAME6CFE0 |Landing marker comparison reads the just-copied unchanged height, so its2C=FF store is unreachable; actor12 can clearA0 without recalculating9C |Physics owner, ordered-store proof and source invariant |
|GAME706E4 |Wrapped INT_MIN negation can cause a source division trap or adjacent-table read |Direction helper; no square-root/atan2 replacement or index clamp |
|GAME6801C |A small negative angle snap retains positive modular delta990..1023 for EA; repeated entity-table aliases update the same player again |`game_player_update.c`, source-address workflow and composed `match_player_update` regression cases |
|GAME6A2E4/2AB70 |Argument0 still loads the threshold and consumes shared RNG; a later rejection keeps the draw; accepted requests can be skipped by the actual motion gates |`game_player_jump.c`, actual motion-callee comparisons and `game_player_jump_workflow.md` |
|GAME61760 |Rejected jumps retain child effects and RNG before fallback; phase81 fallback keeps old velocities while other phases quarter them with signed downward rounding |`game_player_input.c`, actual jump/motion/queue compositions and `game_player_input_workflow.md` |
|GAME700E4/7A498 |A negative previous halfword suppresses high bits in the full32 edge result; contradictory directions keep literal bit priority; controller2A is written twice |`game_player_input.c`, exhaustive prior-mask and raw-direction source comparisons; native `match_input_edges` preserves both masks and writes |
|GAME5BC34/602CC continuation |Tip receiver index adds unsigned side+3 and one RNG bit without multiplying side by5; the phase transition repeats both jumper cooldown stores and reloads the current team after child calls |`game_tipoff_phase.c`, exact source comparisons and [tipoff workflow](game_tipoff_phase_workflow.md); loose-ball release is not retained possession |
|GAME58610 |Phase81 can still consume accuracy-error RNG; the vertical trajectory entry is read after horizontal ball stores, so aliases can change it; original signed division traps retain prior mutations |`game_ball_release.c`, source/prefix comparisons and [ball-release workflow](game_ball_release_workflow.md) |
|GAME54D4C/54ED8/54E50 |Zero and most negative counts still process one quad; fixed textured projection prefetches past the last quad; the cull check reads LZCR rather than FLAG and rejected packet fields remain stale |`game_court_packets.c`, integer geometry and [court workflow](game_court_packets_workflow.md); no padding or replacement flag check |
|GAME55368 |The final part still prefetches the next parent pointer beyond a context-only allocation; mirror signs, endpoint truncation and matrix-padding writes retain their original order |`game_player_geometry.c` and [player workflow](game_player_geometry_workflow.md); reached reads are not removed or padded, and native refusal retains preceding stores |
|GAME5200C/56080/51F18 |Euler terms negate low32 products before rounding; scale/index rereads retain aliases; the final matrix word includes sign-extended padding |`game_player_root.c` and [root matrix workflow](game_player_root_workflow.md); all37 stores and refusal prefixes preserve source order |
|GAME525AC/54660/5483C/54A18/54ADC |Zero/one counts retain extra triangle iterations; final corner prefetches remain; biased and alternate links use different widths and store order |`game_player_projection.c` and [projection workflow](game_player_projection_workflow.md); original winding, depth arithmetic and refusal prefixes retained |
|GAME51098/56080/8F224 |Euler negation precedes rounding; final matrix translation changes without updating retained geometry translation; cached input does not rewrite its aggregate |`game_camera.c` comments and [camera workflow](game_camera_workflow.md); exact ordered effects, byte knowledge and all64 geometry words compared |
|GAME52914/4C5E8 |Actor loop reloads its mutable index; indicator's early negative selection check becomes exactly -1 at the late check, so an updated packet can remain unlinked |`game_player_frame.c` comments and [player pass workflow](game_player_frame_workflow.md); original caller and shadow/indicator comparisons retain prior mutations |
|GAME479B8 geometry tail |Zero counts still reach list allocations; line packets initialize only tag length/command/RGB; live count/cursor rereads preserve aliases |`game_court_resources.c` and [resource workflow](game_court_resources_workflow.md); payload links/XY remain incoming, allocator flags0 preserved |
|GAME479B8 texture loop |Four-bit palette widths compare as signed and clamp only above16; palette publication/clamping precedes imageXY reads; count is reread after uploads |`game_court_textures.c` and [court texture workflow](game_court_textures_workflow.md); no repaired width, cached count or rollback |
|GAME9AC7C |Nonpositive upload dimensions clamp to zero and returnFFFFFFFF without pixel/GPU access; the image caller ignores that return |`game_render_backend.cpp`;729 original-instruction cases and an actual court texture exercise this no-data boundary |
|GAME4AC68/56914 |Live group visibility and cursors are reread; fixed textured and generic tag insertion use different store order; even an unused flat argument is read before projection |Same court owner; ordered-memory and alias/prefix comparisons |
|GAME539FC/35A44 |Jersey FF draws texture00 but labels display -1; a palette with no zero entry uses fill byte10 rather than a repaired transparent index |Render texture/label owners, original-instruction comparisons and `game_render_entry_workflow.md` |
|GAME50768 |Both polygon banks use bank A's captured high-byte group even when B specifies another group; signed wrapped counts, raw offsets and repeated pointer relocation are not repaired |`game_body_geometry.c`, ordered original-instruction comparisons and `game_body_geometry_workflow.md`; retained native ownership preserves aliases |
|GAME504A8 tail |All six name-UV writes reuse the saved old first center, including the second packet; the old second center is never consumed, and reversed odd endpoints round downward |`game_body_names.c`, exact source order and alias comparisons; `game_body_names_workflow.md` |
|GAME4E3CC/35A44 |Name character count wraps at8 bits; original label formatting can overflow its32-byte stack buffer |Original widths retained; explicit native ownership/overflow refusal instead of truncation, with prior writes preserved |
|GAME30758/30D18/2EF88 |Reset deactivates without freeing; exhausted object scans still reuse the reached slot and can create a self-link; the bitmap limit permits equality and zero glyphs can return a real pointer without bitmap writes |`game_text_objects.c`, whole-source comparisons and `game_text_objects_workflow.md`; native loop bounds refuse without repairing cycles |
|GAME2E528 |Glyph descriptors inherit the incoming packet tag's low24 bits; image header links are cleared before upload and rebuilt only for certain replacement logos; NULL resource attempts retry unchanged |`game_font_loader.c`, retained scratch knownness, original five-font resource comparisons and `game_font_loader_workflow.md`; no fabricated terminator, automatic link restoration or successful empty font |
|GAME2E200/90160/901EC |Allocation wrappers dereference the returned descriptor even when NULL; the lock target is reread after allocation; B2048 is published before the final capacity and bitmap writes, whose live base can change through aliases |`game_text_pools.c`, exact store/callback comparisons and `game_text_pools_workflow.md`; untouched pool bytes retain their incoming state and native refusal preserves prior writes |
|GAME8FA6C/8FB4C/90CE4 |Zero or negative descriptor counts still write a terminal link; sentinel formatting precedes descriptor field stores; the final unlock uses the current pointer even when NULL |`game_heap_initialize.c`, original-instruction and prefix comparisons; payload bytes and untouched descriptor fields are not implicitly cleared |
|GAME9027C/90D40 |Empty free lists are dereferenced without a NULL check; wrapped gaps and sizes compare as signed; descriptor pop and serial updates survive name-copy failure; later link and guard fields are reread live |`game_heap_allocate.c`, original-instruction comparisons and `game_heap_allocate_workflow.md`; allocation retains unknown payload bytes and does not repair source lists |
|GAME38A18 |Cache searches have no original bound; count is checked after service; home bench swaps synchronize three times while away swaps omit those calls |Head-cache owner, original ordered callback comparisons and mutable-cache probes; owned-range refusal stays explicit |
|GAME94440/94540/946B8 |Odd rectangle width forces height odd; split uploads temporarily decrement the shared header height; CLUT placement0,0 still mutates the header while suppressing its transfer |`game_image_upload.c`; source comparisons and composed native VRAM tests preserve ordered prefixes without automatic header repair |
|GAME29E58 |A zero `zloadscr.psh` archive handle silently skips lookup, synchronization, drawing and release, but a zero `LdS1` image result has no guard and still reaches all three image uploads |`game_loading_screen.c` source comments, null-path unit cases and [loading-screen workflow](game_loading_screen_workflow.md); no symmetric null policy or compensating release added |
|FE2F36C/6B6A0 |Router retains a zero-status branch although the original BUSY helper cannot return zero |`music_routing.c`, source-quirk comment and lifecycle tests |
|FE2F330 |The sixteen-slot music table permits repeats and includes the special track in ordinary selection |Music extractor/routing workflow; no guessed shuffle |
|FE7B2BC/7A81C |A long fade can compute step0 and stall; envelope countdownFFFFFFFF decrements and short stages can divide by zero |`music_voice.c`, source service/fade comparisons and workflow |
|FE72254/72954 |Only full1024-byte/channel staging blocks are submitted; all five tracks discard their partial final block |`music_stream.c`, all original track chunk comparisons and workflow |
|FE7309C |Natural drain compares signed read index with write index minus one without modulo correction; ending at write index0 can miss key-off |Stream stop/advance helpers and full original IRQ comparisons |
|FE31A88 music arm |Zero adjacent volume skips reduction; repeated View Player entry overwrites saved volume; exit restores the saved low byte even after intervening edits |`music_transition.c`, exact source state/callback comparisons; no extra gain call to cancel the original fade |
|FE3122C/8ABF0 |Already-finished nonnegative announcer voice keeps its old value; CRCF validation ignores the trailer's length field |`frontend_resource.c`, actual lifetime/content comparisons; native callback obligations remain explicit |
|FE313C8/2FB00 |Already-finished announcer handle stays stale; portrait data with a zero graphic field is discarded without a free; cached record IDs survive cleanup |`frontend_resource_cleanup.c`, ordered source/callback comparisons and explicit source-bug comments |
|FE916CC/92BFC/7AD48 |A status query returns its saved pre-unlock result even if pending service clears the voice during that unlock; unlocking depth0 wraps instead of repairing the counter |`voice_handles.c`, source comparisons and deferred-service tests |
|FE702B0 |A nonzero channel-change guard returns with busy1; key batches use kind==2 while state4 completion uses kind>=2; post-callback mask rereads retain live changes before clearing the mask |`voice_channels.c`, all373 original instructions covered and composed fade/retirement tests |
|FE91338/912E8 |Generation advances on failure; default priority102 can select voice0 outside the mask; a zero count can publish a stale scratch selection; independent membership counts permit duplicate selection |`voice_allocation.c`, whole allocation/membership and real nested handle-service comparisons |
|FE91338 |The new handle is written before STOP/STATUS; failed stealing retains that handle, queued keyoff and earlier activations; final unlock can retire a just-selected voice before return |Same owner and `voice_allocation_workflow.md`; no rollback or invented hardware retirement |
|FE919A0/92628 |Registration initializes only the first word of its sample-mapping table and ignores its contents after upload, keeps the pre-callback bank slot, returns constant8 after any nonnegative upload, and does not check enabled |`voice_programs.c`, mutable-callback source comparisons and `voice_programs_workflow.md`; incoming table bytes and upload changes remain intact |
|FE924B4/91AB4 |First-tone upload failure still marks the relocated header loaded while returning the negative result; later failure keeps relocations, and unload never clears loaded or restores relative pointers |`voice_patl_upload.c`, explicit original-bug comment, composed registration comparisons and `voice_patl_upload_workflow.md`; missing mapping backends refuse without inventing successful uploads |
|FE70884/714B8 |The right-channel transfer requests the right size but compares completion with the left size; failure can retain allocated SPU storage; appended mapping records receive no new terminator; unload ignores free errors and retains fields |`voice_mapping.c`, original-instruction comparisons and `voice_mapping_workflow.md`; missing allocation, transfer and event owners remain explicit boundaries |
|GAME49300/49D34 |Animation uses wrapped increment and signed remainder, including negative frames; reflected size lacks the ball's minimum clamp; reflection uses primary ball depth and reloads the bank/OT after the first link |`game_ball_frame.c` source comments and [ball workflow](game_ball_frame_workflow.md); original instruction, alias and retained-prefix comparisons |
|GAME4D490/4CAF4/50E74/50F88 |Repeated format/page reads remain; UV dimensions wrap; numeric resource globals survive release; unused packet padding and arrow color bytes remain unwritten |`game_player_marker_resources.c` comments and [marker workflow](game_player_marker_resources_workflow.md); no source resource-lifetime extension or fabricated packet fields |
|GAMEA547C/50F54/51064 |SHPP search prefetches beyond its last directory entry; a missing image link still leads to palette reads at offsets12/14 from zero |Marker-resource owner preserves ordered reads and preceding writes; unowned native memory refuses without inventing a default palette |
|GAME57F5C/58120/581C0 |Mode one writes X/Z and returns hand height without storing ball height; the other modes write all coordinates. Wrapped interpolation and original arithmetic traps remain |`game_ball_attachment.c` comments and [attachment workflow](game_ball_attachment_workflow.md); original/normalized-adapter comparisons preserve exact prefixes |
|GAME4B1A4/4B6C4/4C144 |Reinitialization relocates offsets again; rendering precedes animation/deformation; only one matrix row scales; 96 of 100 initialized net lines are drawn |`game_net.c` comments and [net workflow](game_net_workflow.md); no repaired counters, uniform scaling or fabricated packet bytes |
|GAMEAA168 |Declared output length does not bound decoding; overlapping backreferences consume newly written bytes |Net decoder preserves source order and unknown payload knowledge, with explicit native ownership/operation limits |
|GAME49018 |Style-byte store precedes sync in its delay slot; actor index and submission pointers are reread; scratch redraw repeats rendering without advancing the outer game loop |`game_match_frame.c` and [frame workflow](game_match_frame_workflow.md); mutable-callee and original-instruction comparisons |
|GAME479B8 startup bridge |Texture selection captures its selector before sentinel writes; geometry selection rereads it after free; duplicate filename slots and unknown special-packet padding survive |`game_court_startup.c` and [startup workflow](game_court_startup_workflow.md); real native heap release retains source mutation prefixes |
|GAME530FC/54FCC |Angle conversion executes at least once for a signed zero or negative count; opaque partial source words become wholly unknown after the original register load/store |`game_pose_frame.c` comments and [pose-frame workflow](game_pose_frame_workflow.md); original CPU memory/knownness comparison |
|GAME4A044 |Controller intensity clamps zero to one, while the computer-player route deliberately leaves zero intensity; index, bank and actor inputs are reread around upload/sync |`game_player_marker_update.c` and [marker-update workflow](game_player_marker_update_workflow.md); composed VRAM and original comparisons |
|GAME479B8 roster/interactive |Scratch `+14` bit handling writes its XOR result to `+4`; raw heights 200..255 render a fixed hundreds glyph of one before subtracting only 100, displaying 200 as 100 and 255 as 155 before clamp |Roster and interactive owners, [roster workflow](game_court_roster_startup_workflow.md) and [interactive workflow](game_court_interactive_workflow.md); exhaustive source comparisons |
|GAME9BF98/993DC |A first graphics-mode value of two does not select the alternate page encoding; it forces a live second read, and only a second value of two selects that route |`game_page_offset.c` and [page-offset workflow](game_page_offset_workflow.md); all 65,536 first/second byte transitions compared |
|GAME6EF60/6D880 |Ball simulation always performs one substep for zero/negative tick counts; saved rim coordinates use a different scale from current coordinates; an upward backboard check can snap height before returning no collision |`game_ball_simulation.c` and [simulation workflow](game_ball_simulation_workflow.md); original instruction and composed trajectory comparisons |
|GAME6DC18 |Scoring response math reuses stale pre-division operands, and a reused stack slot makes a later sign test observe the reflected component rather than its earlier grid coordinate |`game_ball_scoring.c` and [scoring workflow](game_ball_scoring_workflow.md); original CPU comparisons and trap/refusal prefixes |

This is a growing index, not proof every original bug has been found. Callers
and host adapters must preserve these outcomes when integrating the recovered
owners; unit-level preservation alone is not full-game runtime verification.

Unknown original state must not become a guessed zero or default player.
Original out-of-array reads are a separate native storage boundary: refuse
explicitly when the required owned state is unavailable, without claiming the
original would have refused or succeeded. A source divide trap is different:
its verified effects before the trap are retained. See each owner for whether
a native refusal is atomic or retains a completed prefix.

Port-language undefined behavior is not an original PS1 bug. For example,
replacing C++ negative-left-shift expressions in the SCHl decoder with defined
arithmetic preserves the existing PCM output; it does not authorize changing
the predictor rounding to match another decoder.
