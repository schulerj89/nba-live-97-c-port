#ifndef NBA97_GAME_PLAYER_LABEL_FRAME_H
#define NBA97_GAME_PLAYER_LABEL_FRAME_H
#include "game_player_frame.h"
#ifdef __cplusplus
extern "C" {
#endif
/* Complete35BEC actor-linked text-object pass,55F0C scratchword read and
 * 2F10C packet-allocation bitmap release. Reuses actual56914 linker in its
 * established aligned-tag domain. No math/child callback, font substitution,
 * object creation, countdown decrement or implicit pool initialization.
 * Uses original live addresses and retained aliases. Cyclic/negative-length
 * paths stop only at native budget/ownership refusal; no list/count repair.
 * Source private ABI stack/code may not alias visible allocations. All
 * completed writes remain on refusal; this is not a resumable cursor.
 * Progress actors counts completed outer actor slots; indicators counts
 * visited text objects; links counts completed source packet links.
 */
int nba97_game_player_label_frame(Nba97PlayerFrameContext*,Nba97PlayerFrameProgress*);
int nba97_game_player_label_frame_release_packets(Nba97PlayerFrameContext*,
    uint32_t packet,uint32_t count,Nba97PlayerFrameProgress*);
#ifdef __cplusplus
}
#endif
#endif
